# Palme Odoo — متابعة المشروع (PROJECT TRACKER)

> ملف واحد بيوثّق المطلوب وحالة كل بند. بعد أي شغل بنخلّصه، بنحدّث الحالة + نضيف سطر في السجل.
> آخر تحديث: 2026-06-24

---

## الاحتياجات الأصلية (كما طُلبت في بداية المشروع)

النظام: داشبورد مراقبة مالية وتشغيلية على Odoo 17 — **قراءة فقط (read-only) بدون أي استثناء**.

| # | المنطقة | المطلوب باختصار | الحالة |
|---|---------|------------------|--------|
| 1 | **مراقبة جلسات نقاط البيع (POS)** | معرفة هل الجلسات بتتقفل بانتظام يوميًا (2–3 مرات) ولا فيه جلسات بتفضل مفتوحة بالأيام | ✅ **خلصت** |
| 2 | **مطابقة مدفوعات الفيزا** | اللي بيتحصّل بالفيزا عند الكاشير — هل المحاسب بيطابقه مع الحساب البنكي ويسجّله؟ وإيه اللي مش بيتسجّل بشكل دوري | ✅ **خلصت** |
| 3 | **الحركات البنكية عمومًا** | كل الحركات البنكية وهل فيها فجوات ولا مظبوطة (الكل المفروض متسجّل) | ⬜ **اللي جاي** |
| 4 | **فجوات دورة المشتريات** | فاتورة ← استلام ← Bill: هل كل فاتورة اتعملها Bill؟ إيه اللي ماتعملوش؟ وهل فيه Bill اتكرر أكتر من مرة؟ | ⬜ لم تبدأ |

---

## التفاصيل والاكتشافات لكل بند

### 1) مراقبة جلسات نقاط البيع — ✅ خلصت
- شاشة فيها: كروت مؤشرات + جدول الجلسات المفتوحة (ملوّن بالخطورة) + جدول انضباط الإغلاق لكل فرع + رسم بياني.
- فلتر الشركة بيفلتر الصفحة كلها؛ فلتر التاريخ بيأثر على جدول الفروع بس (الجلسات المفتوحة لحظية).
- **أهم اكتشاف:** فيه جلسات مفتوحة بالشهور/السنين (أقدمها 614 يوم وعليها 1,360 طلب)، و12 جلسة حالة حرجة، وجلسات "إنقاذ" (RESCUE) بتتولّد لسه لحد دلوقتي وبتفضل مفتوحة — محتاجة تدخّل تشغيلي مع فروع التحصيل.

### 2) مطابقة مدفوعات الفيزا — ✅ خلصت
- الشاشة اتبنت واتأكد منها: لكل فرع — المُحصَّل مقابل المؤكَّد من Geidea مقابل المعلّق.
- قاعدة التأخير FIFO: يومين عمل من `stop_at` بتاع الجلسة، مع اعتبار الجمعة فقط إجازة نهاية أسبوع.
- فلتر الشركة بيفلتر الصفحة كلها.
- كومة الشركة القديمة (company-1) ~107M متوقّفة من سبتمبر-2025، بتتعرض كـ "للعِلم فقط" (awareness-only).
- **إشارة التأكيد:** قيد يدوي (JE) بيعمل credit لحساب وسيط الفيزا (holding account) مع طرف مقابل "Liquidity Transfer" — حساب 63001 لـ #بالميه#./co3، وحساب 101701 لـ co1 — وبتتحل لكل شركة على حدة.
- **اكتشافات أساسية (للسياق):** `is_matched` في `account.payment` غير موثوق كمؤشر للمطابقة البنكية؛ والفيزا مش بتتنسب لفرع عن طريق الحساب وحده (الشركة بتشارك نفس الحساب عبر فروع) — لازم عبر الـ journal / طريقة الدفع.

**الأفق / اللي جاي:** إضافة drill-down: الضغط على معلّق الفرع يفتح تفصيل الجلسات المكوّنة للمبلغ.

### 3) الحركات البنكية — ⬜ اللي جاي
- ملاحظة من الـ Discovery: `account.bank.statement` غير مستخدم (نستخدم `account.bank.statement.line.is_reconciled`)، ومعظم السطور خزن نقدي مش بنوك حقيقية.

### 4) دورة المشتريات — ⬜ لم تبدأ
- ملاحظة من الـ Discovery: `invoice_count` موثوق؛ ~4,552 أمر شراء "تم استلامه بدون فاتورة" (فجوة GR/IR)؛ حقول `purchase_id` المحسوبة بحثها غير موثوق — نعتمد على الحقول المخزّنة.

---

## سجل الإنجاز (Changelog)

- **2026-06-24** — Area 2 (مطابقة تحصيل الفيزا) اكتملت واتأكد منها — فلتر الشركة، قاعدة FIFO، وقسم الأرصدة القديمة. + إصلاح فلتر الشركة ليشمل الصفحة كلها.
- **2026-06-23** — Area 1 (مراقبة جلسات POS) اكتملت واتأكد منها بالكامل.
- **2026-06-23** — إصلاح توقّف الباك إند عند الإقلاع (قيمة `PYTHONUTF8` كان فيها مسافة زيادة في `run-all.bat`).
- **2026-06-23** — إصلاح فلتر الشركة بحيث يفلتر صفحة POS كلها.
- **2026-06-23** — **إصلاح جذري:** الاتصال المشترك بـ Odoo بقى آمن تحت الضغط (قفل تنظيم للطلبات) — حلّ كمان تذبذب التقارير القديمة. طبقات الأمان السبعة (read-only) اتأكد إنها سليمة.

---

## الثوابت والقواعد (لا تتغيّر)
- **قراءة فقط من Odoo — بدون أي كتابة، تحت أي ظرف.** (7 طبقات حماية في `OdooReadOnlyClient`).
- أي تغيير = إضافي/أقل ما يمكن، بدون المساس بالكود الحساس أو `.env`.
- بعد أي تعديل حساس: إعادة تشغيل اختبار الأمان للتأكيد.
- التوثيق: بعد كل مهمة نخلّصها → نحدّث هذا الملف (الحالة + سطر في السجل).